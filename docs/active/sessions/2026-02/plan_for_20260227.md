# Session Plan: Epic Sandbox Login – Complete OAuth Flow (2026-02-27)

Copy and paste the prompt below to start the next session focused on finishing the Epic sandbox OAuth flow and FHIR testing.

---

## Chat Prompt (Copy-Paste Ready)

```
Continue Epic Phase 2 sandbox connectivity. OAuth flow gets to the Epic Hyperspace login page (HSWeb_uscdi) but we need to complete the flow successfully.

Context:
- App: EMS Transport Companion. Non-Production Client ID: 80c917fc-f6fe-4b9f-91cd-89a09f1cbbcd
- EPIC_SKIP_AUD=1 is set in backend/.env.local (required to avoid "Invalid OAuth 2.0 request")
- Redirect URI http://localhost:5001/api/epic/callback is confirmed in Epic portal
- User was using Epic UserWeb credentials (chuckf43) – WRONG. Should use sandbox test credentials.
- Full session context and troubleshooting steps are in docs/active/sessions/2026-02/plan_for_20260227.md

Please:
1. Update epic_app_config_steps.md to correct the login credential guidance (sandbox test creds, not UserWeb)
2. Help complete the OAuth flow: user should use sandbox test credentials at the Hyperspace login, then select a patient
3. After successful OAuth, verify callback redirect and test FHIR reads
4. Document the working credentials and flow in the app config doc
```

---

## Summary: What We’ve Done So Far

### Phase 2 Implementation (Complete)

- **Branch:** `feature/epic-phase2-sandbox`
- **OAuth2 + PKCE:** `epicAuthService.ts` – authorize URL, token exchange, refresh
- **FHIR Service:** `epicFhirService.ts` – Patient, Encounter, Condition, ServiceRequest, Location, Organization
- **Routes:** `/api/epic/auth`, `/api/epic/callback`, `/api/epic/auth/url`, FHIR resource endpoints
- **Env loading:** Explicit paths for `.env.local` so it loads reliably from backend dir

### OAuth Flow Progress (2026-02-25 through 2026-02-26)

1. **Client ID fix:** Switched from Production ID (`878e5c65`) to Non-Production ID (`80c917fc`) – sandbox requires Non-Production.
2. **Env path fix:** `.env.local` was not loading; now uses `path.resolve(__dirname, '..')` in both `index.ts` and `databaseManager.ts`.
3. **"Invalid OAuth 2.0 request":** Resolved by adding `EPIC_SKIP_AUD=1` (Epic was rejecting the `aud` parameter).
4. **Login credential confusion:** User tried Epic UserWeb credentials (fhir.epic.com Build Apps). Those caused a stall. **Sandbox requires sandbox test credentials**, not UserWeb.
5. **Redirect URI:** Confirmed `http://localhost:5001/api/epic/callback` is set in Epic portal.

---

## Current State (End of 2026-02-26 Session)

### What Works

- `http://localhost:5001/api/epic/auth` redirects to Epic OAuth
- Epic shows the Hyperspace (HSWeb_uscdi) login page – no "Invalid OAuth 2.0 request"
- Redirect URI is correct in Epic portal
- Backend sends full scopes (patient/Patient.read, patient/Encounter.read, etc.)
- Backend healthy; `EPIC_CLIENT_ID` loads correctly

### What’s Blocking

- **Login:** User must use **sandbox test credentials**, not Epic UserWeb.
- **Expected flow:** Hyperspace login → patient picker → Epic redirects to our callback with `code` → we exchange for tokens → redirect to frontend.

### Backend Env (backend/.env.local)

```env
EPIC_CLIENT_ID="80c917fc-f6fe-4b9f-91cd-89a09f1cbbcd"
EPIC_CLIENT_SECRET="<set>"
EPIC_FHIR_BASE_URL="https://fhir.epic.com/interconnect-fhir-oauth/api/FHIR/R4"
EPIC_REDIRECT_URI="http://localhost:5001/api/epic/callback"
EPIC_SKIP_AUD=1
# EPIC_USE_MINIMAL_SCOPES – leave unset
```

---

## Next Steps: Troubleshoot Login & Complete Flow

### 1. Use Sandbox Test Credentials

The Hyperspace login expects **sandbox test credentials**, not Epic UserWeb.

**Standard Epic sandbox (from web search):**
- Usernames: `fhirjason`, `fhirdaisy`, `fhirderrick`, `fhirdesiree`, etc.
- Password: `epicepic1`

**Documentation source:** [Sandbox Test Data](https://fhir.epic.com/Documentation?docId=testpatients) on fhir.epic.com (login required). It lists both test patient IDs and login credentials.

**USCDI sandbox (HSWeb_uscdi):** This flow uses the USCDI environment. If `fhirjason` / `epicepic1` fail, check the Sandbox Test Data doc for USCDI-specific clinician credentials, or contact Epic support.

### 2. After Login: Patient Picker

For clinician apps with `patient/*` scopes, Epic shows a **patient selection** step after login. The user must select a sandbox test patient (e.g. `erXuFYUfucBZaryVksYEcMg3`) before Epic redirects to our callback.

### 3. Verify Callback

After patient selection, Epic redirects to:
`http://localhost:5001/api/epic/callback?code=...&state=...`

Our backend exchanges `code` for tokens and redirects to:
`http://localhost:3000/auth/epic/callback?success=1&patient=...`

Confirm that `/auth/epic/callback` exists in the frontend or behaves correctly.

### 4. Test FHIR Reads

After a successful OAuth flow, call:
- `GET /api/epic/Patient/{patient-id}` (use the selected patient ID)
- Or other FHIR endpoints as in `epicFhirService.ts`

---

## Files to Update Next Session

| File | Action |
|------|--------|
| `docs/active/features/epic_app_config_steps.md` | Fix login credential guidance: use sandbox test creds (fhirjason/epicepic1), not UserWeb. Update "Invalid OAuth 2.0 request" and "Invalid user" sections. |
| `docs/active/sessions/2026-02/plan_for_20260227.md` | Update with results after next session (working credentials, any new issues). |

---

## Key Files for Context

| File | Purpose |
|------|---------|
| `backend/src/services/epicAuthService.ts` | OAuth URL builder, token exchange, refresh. Uses `EPIC_SKIP_AUD` to omit `aud` when set. |
| `backend/src/routes/epic.ts` | `/auth`, `/callback`, `/auth/url`, FHIR routes. Callback redirects to `${FRONTEND_URL}/auth/epic/callback`. |
| `docs/active/features/epic_app_config_steps.md` | Step-by-step Epic config and troubleshooting. |
| `.cursor/plans/epic_integration_plan_30d74051.plan.md` | Phase 2 TODOs. OAuth flow verified; FHIR reads pending. |

---

## Restart Command

```bash
bash documentation/scripts/start-dev-complete.sh
```

Or: `cd backend && npm run dev` (and separately for frontend).

---

## Epic Portal Links

- [Build Apps](https://fhir.epic.com/Developer/Apps)
- [EMS Transport Companion edit](https://fhir.epic.com/Developer/Edit?appId=51315)
- [Sandbox Test Data](https://fhir.epic.com/Documentation?docId=testpatients)
- [OAuth 2.0 Tutorial](https://fhir.epic.com/Documentation?docId=oauth2)
